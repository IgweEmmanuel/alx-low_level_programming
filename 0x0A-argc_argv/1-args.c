#include <stdio.h>

/**
 * main - This prints the argument names in the compiler
 * @argc: argument 1
 * @argv: argument 2
 *
 * Return: outputs ints
 */
int main(int argc, char __attribute__ ((unused)) *argv[])
{

	printf("%d\n", argc);

	return (0);
}
