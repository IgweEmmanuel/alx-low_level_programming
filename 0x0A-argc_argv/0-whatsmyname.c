#include <stdio.h>

/**
 * main - This prints the argument names in the compiler
 * @argc: argument 1
 * @argv: argument 2
 *
 * Return: outputs ints
 */
int main(int __attribute__ ((unused)) argc, char *argv[])
{

	printf("%s\n", argv[0]);

	return (0);
}
