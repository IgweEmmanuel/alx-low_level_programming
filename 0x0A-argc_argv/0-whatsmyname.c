#include <stdio.h>

/**
 * main - This prints the argument names in the compiler
 *
 * Return: outputs ints
 */ 
int main(int __attribute__ ((unused)) argc, char *argv[]){

	printf("%s\n", argv[0]);

	return (0);
}
