#include "main.h"
/**
 * _islower - Checks if a character is in lower case
 * @i: This is the character
 * Return: Returns 0 or 1
 */
int _islower(int i)
{
if (i >= 'a' && i <= 'z')
return (1);

return (0);
}
